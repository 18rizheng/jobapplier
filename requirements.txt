# Install order matters: python-jobspy pins numpy==1.26.3 which has no wheel
# for Python 3.13+. Install these first, then jobspy WITHOUT its pinned deps:
#
#   pip install -r requirements.txt
#   pip install python-jobspy --no-deps
#
numpy
pandas
requests
beautifulsoup4
pydantic
tls-client
markdownify
regex
